<div class="navbar-lewagon">
    <%# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> %>

        <% if user_signed_in? %>
          <div class="nav-item mx-2">
            <%= link_to image_tag("add_record.svg"), new_record_path, class: "nav-link" %>
          </div>
          <div class="nav-item active mx-2">
            <%= link_to image_tag("allrecords.svg"), records_path, class: "nav-link" %>
          </div>
          <div class="nav-item mx-2">
            <%= link_to image_tag("profile.svg"), "/profile", class: "nav-link" %>
          </div>
        <% else %>
          <div class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </div>
        <% end %>
</div>
